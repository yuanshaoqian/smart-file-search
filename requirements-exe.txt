# 打包专用依赖
# 核心依赖
PyQt6>=6.5.0
whoosh>=2.7.4
python-docx>=1.1.0
openpyxl>=3.1.2
PyYAML>=6.0
loguru>=0.7.2
watchdog>=3.0.0
chardet>=5.2.0

# AI 功能（集成到打包应用中）
# 用户只需提供 GGUF 模型文件
# 注意：llama-cpp-python 需要预编译 wheel 或从源码编译
llama-cpp-python>=0.2.23
numpy>=1.24.0
typing_extensions>=4.5.0

# PyInstaller 需要的依赖
pyinstaller>=6.0.0
pyinstaller-hooks-contrib>=2024.0

# Windows 特定依赖
pywin32>=306; sys_platform == "win32"
